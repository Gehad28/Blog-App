export interface User {
    id: string | null,
    name: string | null,
    email: string | null,
    password: string | null,
    pic: string | null,
    bio: string | null,
    facebookUsername: string | null,
    phone: string | null
}